Please change all paths to get worked with the scripts.
All paths are just EXAMPLES!

InitializeDatabase.ps1: Setting the database up with SchemaValidation.js, Index.js and AccessControl.js (by default off)

MongoBackup.ps1: Creating a backup

MongoRestore.ps1: Restoring a backup

WindowsTaskplanner.ps1: Creating a Windows Taskscheduler with the MongoBackup.ps1 File 